import styled from 'styled-components';

import { Popup as P } from '../Popup';

export const Popup = styled(P)`
  .rec-popupContent {
    width: 90%;
    max-width: 900px;
    margin: 100px auto;
    padding: 30px;
  }
`;
